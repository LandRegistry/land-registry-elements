{
  "name": "flask-skeleton-ui",
  "version": "1.0.0",
  "description": "This repository contains a flask application structured in the way that all Land Registry flask user interfaces should be structured going forwards.",
  "scripts": {
    "test": "gulp test",
    "build": "gulp copy && gulp build",
    "dev": "npm run build && gulp watch"
  },
  "repository": {
    "type": "git",
    "url": "git@192.168.249.38:skeletons/flask-skeleton-ui.git"
  },
  "dependencies": {
    "govuk-elements-sass": "3.0.2",
    "govuk_frontend_toolkit": "5.2.0",
    "govuk_template_jinja": "0.19.2",
    "land-registry-elements": "github:landregistry/land-registry-elements#experimental-simplified-version",
    "jquery": "1.12.4"
  },
  "devDependencies": {
    "land-registry-gulp-tasks": "LandRegistry/land-registry-gulp-tasks",
    "gulp": "^3.9.1"
  }
}
