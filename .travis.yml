language: node_js
node_js:
  - "6"
before_install:
  - npm install -g mocha
  - npm install -g eslint

cache:
  directories:
    - node_modules

addons:
  artifacts:
    s3_region: "eu-west-1"
    paths:
      - test/fixtures/visual-regression
